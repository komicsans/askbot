server {
    listen 80 default_server;
    root /var/www/html;
    location /m {
        alias /site/static;
    }
    location / {
        try_files $uri @uwsgi;
    }
    location @uwsgi {
        include uwsgi_params;
        uwsgi_pass unix:///tmp/uwsgi.sock;
    }
}